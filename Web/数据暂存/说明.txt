数据暂存设计， 当数据库不能访问的时候使用数据暂存以读写文件的方式来修改,当数据库恢复时再将数据写入到数据库中

将按不同类别分出不同的书名，书名目录下包括章节 每一个 类型，书名，章节分别对应数据库中的一条记录，当数据更新时同步更新文件。文件采用延改写入的方式 一个小时才会写入一次，更新文件
2.为了保证文件大小，采用 zip 压缩保存

类别	
		校园歪歪
				小猪快跑
						章节1，章节2